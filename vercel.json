{
  "version": 2,
  "installCommand": "cd client && pnpm install",
  "buildCommand": "cd client && pnpm run build:client",
  "outputDirectory": "client/dist/spa",
  "framework": "vite",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "client/dist/spa/$1"
    }
  ]
}
