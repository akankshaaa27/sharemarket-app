{
  "version": 2,
  "buildCommand": "cd client && pnpm install && pnpm run build:client",
  "outputDirectory": "client/dist/spa",
  "framework": "vite",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
