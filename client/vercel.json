{
  "version": 2,
  "buildCommand": "cd client && pnpm install && pnpm run build",
  "outputDirectory": "client/dist",
  "framework": "vite",
  "routes": [
    { "src": "/(.*)", "dest": "client/dist/$1" }
  ]
}
